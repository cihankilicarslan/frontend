import React from 'react';
import QrCodeGenerator from '../components/QrCodeGenerator';

const QrCodeGeneratorScreen = ({ navigation }) => {
  return <QrCodeGenerator navigation={navigation} />;
};

export default QrCodeGeneratorScreen;
