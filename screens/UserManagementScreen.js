import React from 'react';
import UserManagement from '../components/UserManagement';

const UserManagementScreen = ({ navigation }) => {
  return <UserManagement navigation={navigation} />;
};

export default UserManagementScreen;
